import { Menu } from '../../components';
import MainCanvas from './MainCanvas';

function Whiteboard() {
   return (
      <>
         <Menu />
         <MainCanvas />
      </>
   );
}

export default Whiteboard;
